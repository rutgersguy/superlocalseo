# Copy to .env.test for the isolated test stack (docker-compose.test.yml).
# Stripe TEST-mode keys only — never live keys. The stack must never be able to
# move real money. Everything else the stack needs is inlined in the compose file.

STRIPE_SECRET_KEY=
STRIPE_PUBLISHABLE_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_SETUP_PRICE_ID=
STRIPE_BASE_PRICE_ID=
STRIPE_LITE_BASE_PRICE_ID=
STRIPE_LOCATION_PRICE_ID=
EMBEDMYREVIEWS_WEBHOOK_TOKEN=
